<%= form_for(@ticket_machine) do |f| %>
  <% if @ticket_machine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ticket_machine.errors.count, "error") %> prohibited this ticket_machine from being saved:</h2>

      <ul>
      <% @ticket_machine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :ticket_seq %><br>
    <%= f.text_field :ticket_seq %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
